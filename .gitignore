# Dependencies — never commit these; they get reinstalled from package.json
node_modules/

# Build output — Hostinger rebuilds this on every deploy
dist/

# Logs
*.log
npm-debug.log*

# Environment secrets — keep passwords/keys out of git
.env

# Editor/tool local settings
.claude/

# OS junk
Thumbs.db
.DS_Store
